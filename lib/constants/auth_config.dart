/// Deep link Supabase uses after email confirmation (and password reset).
///
/// Also add this URL in Supabase → Authentication → URL Configuration → Redirect URLs.
const String kAuthRedirectUrl = 'edu.coc.omr://login-callback';
